{
  "name": "claude-usage-meter",
  "displayName": "Mātrā — Claude + Codex + Gemini Usage",
  "description": "Live Claude, Codex, and Gemini usage — plan limits, tokens and equivalent API cost in one local dashboard.",
  "version": "1.7.5",
  "publisher": "dydxfx",
  "author": {
    "name": "Pandit Floki",
    "email": "pandit@dydxfx.com",
    "url": "https://dydxfx.com"
  },
  "license": "MIT",
  "homepage": "https://dydxfx.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/panditfloki/live-claude-usage-ui.git"
  },
  "bugs": {
    "url": "https://github.com/panditfloki/live-claude-usage-ui/issues",
    "email": "pandit@dydxfx.com"
  },
  "keywords": [
    "claude",
    "claude-code",
    "codex",
    "gemini",
    "antigravity",
    "usage",
    "quota",
    "dashboard",
    "status-bar",
    "anthropic"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "claudeUsage.open",
        "title": "Mātrā: Open Dashboard"
      },
      {
        "command": "claudeUsage.refresh",
        "title": "Mātrā: Refresh Now"
      }
    ],
    "configuration": {
      "title": "Mātrā Usage Meter",
      "properties": {
        "claudeUsage.statusBar.metric": {
          "type": "string",
          "enum": [
            "quota",
            "cost",
            "today",
            "total"
          ],
          "default": "quota",
          "enumDescriptions": [
            "Claude session quota, Codex primary quota, and Gemini primary quota, kept separate",
            "Claude current-window cost, Codex primary quota, and Gemini primary quota",
            "Claude, Codex, and Gemini calendar-today costs, kept separate",
            "Claude, Codex, and Gemini all-time costs, kept separate"
          ],
          "description": "Which figure the status bar shows."
        },
        "claudeUsage.statusBar.show": {
          "type": "boolean",
          "default": true,
          "description": "Show the live burn counter in the status bar."
        },
        "claudeUsage.statusBar.displayMode": {
          "type": "string",
          "enum": [
            "compact",
            "full"
          ],
          "default": "compact",
          "description": "Compact shows provider percentages only; full also shows abbreviated quota reset times."
        },
        "claudeUsage.statusBar.bars": {
          "type": "boolean",
          "default": true,
          "description": "Draw a coloured bar per provider in the status bar. Bars show quota REMAINING, so a full green bar means plenty left. Turn off for percentages only."
        },
        "claudeUsage.statusBar.warningThreshold": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "default": 80,
          "description": "Usage percentage that gives the Mātrā meter a warning background."
        },
        "claudeUsage.statusBar.errorThreshold": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "default": 95,
          "description": "Usage percentage that gives the Mātrā meter an error background."
        },
        "claudeUsage.accounts.claudeAlias": {
          "type": "string",
          "default": "",
          "description": "Optional local label for the active Claude account."
        },
        "claudeUsage.accounts.codexAlias": {
          "type": "string",
          "default": "",
          "description": "Optional local label for the active Codex account."
        }
      }
    }
  },
  "scripts": {
    "dev": "node server.js",
    "package": "vsce package",
    "test": "node --test"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0"
  }
}
